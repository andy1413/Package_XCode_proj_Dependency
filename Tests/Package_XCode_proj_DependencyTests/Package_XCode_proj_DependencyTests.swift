import XCTest
@testable import Package_XCode_proj_Dependency

final class Package_XCode_proj_DependencyTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Package_XCode_proj_Dependency().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
